namespace C4Bank.Deposit.UseCases.SynchronizeNewAccount.UseCase;

public static class SynchronizeNewAccountMapper
{
    public static TTarget Map<TTarget>(object source)
    {
        //TODO: implements mapping class
        return default!;
    }
}